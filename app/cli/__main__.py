# ABOUTME: CLI entry point for running import_data as a module
# ABOUTME: Allows execution via python -m app.cli.import_data

from app.cli.import_data import main

if __name__ == "__main__":
    main()
